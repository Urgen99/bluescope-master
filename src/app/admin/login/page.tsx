import LoginForm from "@/components/admin/LoginForm";

const page = () => {
  return (
    <section className="py-10 md:py-20 md:pt-40 flex items-center justify-center">
      <LoginForm />
    </section>
  );
};

export default page;
